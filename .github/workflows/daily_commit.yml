name: Daily Commit

on:
  schedule:
    - cron: '30 8 * * *'  # Runs daily at 08:30 UTC
  workflow_dispatch:  # Allows manual trigger

jobs:
  commit-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Update daily log - 23f2002677@ds.study.iitm.ac.in
        run: |
          echo "Last updated at $(date)" >> daily_commit_log.txt

      - name: Configure Git
        run: |
          git config user.name "GitHub Action"
          git config user.email "23f2002677@ds.study.iitm.ac.in"

      - name: Commit changes
        run: |
          git add daily_commit_log.txt
          git commit -m "Daily commit from GitHub Action $(date)" || echo "No changes to commit"

      - name: Push changes
        run: git push
